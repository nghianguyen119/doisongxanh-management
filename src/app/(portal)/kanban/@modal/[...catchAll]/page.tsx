/**
 * Soft navigations away from the intercepted task route must clear the modal;
 * without this the last task would stay visible over the next page.
 */
export default function ModalCatchAll() {
  return null;
}
